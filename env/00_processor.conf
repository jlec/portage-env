#BASEFLAGS="-march=corei7-avx -mtune=corei7-avx"
#BASEFLAGS="${BASEFLAGS} -mcx16 -msahf -mno-movbe -maes -mpclmul -mpopcnt -mno-abm -mno-lwp -mno-fma -mno-fma4 -mno-xop -mno-bmi -mno-bmi2 -mno-tbm -mavx -mno-avx2 -msse4.2 -msse4.1 -mno-lzcnt -mno-rdrnd -mno-f16c -mno-fsgsbase"
#BASEFLAGS="${BASEFLAGS} --param l1-cache-size=32 --param l1-cache-line-size=64 --param l2-cache-size=8192"
BASEFLAGS="-march=native"

CFLAGS="${CFLAGS} ${BASEFLAGS}"
CXXFLAGS="${CXXFLAGS} ${BASEFLAGS}"
FFLAGS="${FFLAGS} ${BASEFLAGS}"
FCFLAGS="${CFLAGS} ${BASEFLAGS}"
